# ===========================================
# BOJ Helper - Environment Variables
# ===========================================

# Required: MySQL connection string
DATABASE_URL=mysql://user:password@localhost:3306/boj_helper

# Required: JWT signing secret (32+ chars recommended)
JWT_SECRET=replace-with-strong-secret

# Required for release/mobile builds: always-on API base URL
# Example: https://api.boj-helper.your-domain.com
EXPO_PUBLIC_API_BASE_URL=

# Required for EAS preview/production builds
# Get this from `eas init` or Expo dashboard project settings
EAS_PROJECT_ID=

# Optional: mark this openId as admin
# Example: OWNER_OPEN_ID=boj:your_handle
OWNER_OPEN_ID=

# Required in production: CORS allowlist (comma-separated)
# Example: ALLOWED_ORIGINS=https://app.boj-helper.com,https://preview.boj-helper.com
ALLOWED_ORIGINS=http://localhost:8081

# Optional: external redirect allowlist (comma-separated)
# ALLOWED_REDIRECT_URIS=https://your-domain.com

# Optional: Redis for token blacklist/state cache
# REDIS_URL=redis://localhost:6379

# Optional: force API base URL on client
# EXPO_PUBLIC_API_BASE_URL=http://localhost:3000

# Optional: app identity overrides
# EXPO_PUBLIC_IOS_BUNDLE_ID=com.bojhelper.app
# EXPO_PUBLIC_ANDROID_PACKAGE=com.bojhelper.app
# EXPO_PUBLIC_DEEP_LINK_SCHEME=bojhelper

# Optional: Sentry DSN
# EXPO_PUBLIC_SENTRY_DSN=
